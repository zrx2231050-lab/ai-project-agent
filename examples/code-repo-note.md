# Repository Context

The project will expose a small web app and JSON API. The web app lets users paste project materials, inspect extracted facts, and ask questions.

Progress: local state persistence and retrieval should be enough for the MVP.

Risk: generated answers must cite source snippets so users can verify them.

Todo: add a review stage that flags unsupported claims.
